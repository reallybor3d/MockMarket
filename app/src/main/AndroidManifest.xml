<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MockMarket"
        tools:targetApi="31">

        <!-- MAIN LAUNCHER -->
        <activity
            android:name=".app_ui.StartupPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- extras -->
        <activity android:name=".app_ui.MainActivity" android:exported="false"/>
        <activity android:name=".app_ui.UsernameActivity" android:exported="false" />
        <activity android:name=".app_ui.DashboardActivity" android:exported="false" />
        <activity android:name=".app_ui.MarketActivity" android:exported="false" />
        <activity android:name=".app_ui.PortfolioActivity" android:exported="false" />

    </application>

</manifest>
